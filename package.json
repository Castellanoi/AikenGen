{
  "name": "aikengen",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "electron": "electron .",
    "electron-dev": "concurrently \"npm run start --prefix frontend\" \"wait-on http://localhost:3000 && cross-env NODE_ENV=development electron .\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "cross-env": "^7.0.3",
    "electron": "^32.2.0",
    "wait-on": "^6.0.1"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1"
  }
}
